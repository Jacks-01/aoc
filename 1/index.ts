/**
 * Day 1: 
 */

import { input } from "./input.ts";
console.log('this is our input', input);

// Step 1: Find the first integer in the string

/**
 * 
 * @param {Array} input an array of strings
 */
const getInputValue = (input) => {
    console.log('Does our input look correct?', input);
}

getInputValue(input);

// Step 2: Find the last integer in the string

// Step 3: Sum the first and last integer to get a value for the string

// Step 4: iterate through the remaining ones to get their values as well

// Step 5: Add all of the integers together


